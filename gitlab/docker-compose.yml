
version: '3'
services:
  web:
    image: 'gitlab/gitlab-ee'
    restart: always
    hostname: '5.253.25.50'
    ports:
      - '5080:80'
      - '443:443'
    volumes:
      - '/srv/gitlab/config:/etc/gitlab'
      - '/srv/gitlab/logs:/var/log/gitlab'
      - '/srv/gitlab/data:/var/opt/gitlab'
